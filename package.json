{
  "name": "HoldMyTabsForVsCode",
  "displayName": "HoldMyTabsForVsCode",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.99.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
     "commands": [
      {
        "command": "extension.saveTabsAs",
        "title": "Save Open Tabs As..."
      },
      {
        "command": "extension.restoreTabs",
        "title": "Restore Saved Tabs..."
      },
      {
        "command": "extension.deleteTabs",
        "title": "Delete Saved Tab Sets..."
      },
      {
        "command": "extension.listTabs",
        "title": "List Saved Tab Sets"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.99.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "eslint": "^9.25.1",
    "typescript": "^5.8.3",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2"
  }
}
